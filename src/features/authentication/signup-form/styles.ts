import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  registerButton: {
    marginBottom: '1em'
  },
  returnToLoginLink: {
    textAlign: "center",
    width: "100%",
    display: "block",
  }
})

