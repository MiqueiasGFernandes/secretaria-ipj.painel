const PREFIX = 'RaLogin';

export const StyledFormClasses = {
  card: `${PREFIX}-card`,
  avatar: `${PREFIX}-avatar`,
  icon: `${PREFIX}-icon`,
  content: `${PREFIX}-content`,
  button: `${PREFIX}-button`,
};