import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  container: {
    padding: '10px 20px',
  },
  returnToLoginLink: {
    textAlign: 'center',
    marginTop: '1em',
    width: '100%',
    display: 'block',
  }
})